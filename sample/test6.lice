
; initialize

(if (! (file-exists? "save"))
    (run (write-file (file "save") "0")
         (print "fuck"))
    (print "shit")
)

; read the file and get the value

(set "var" (str->int (read-file (file "save"))))

(print (get "var"))

; write the increased value to file

(write-file (file "save")
            (to-str (+ 7 (get "var")))
)

; evaluate this file

(set "file" (file (to-str (get "FILE_PATH"))))

; (print (get "file"))

(load-file (get "file"))


