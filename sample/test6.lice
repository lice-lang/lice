(if (file-exists? "save")
    (write-file (file "save") "0")
)

(set "var" (+ 1 (str->int (read-file (file "save")))))

(print (get "var"))

(write-file (file "save")
            (to-str (+ 1 (get "var")))
)

; evaluate this file

(set "file" (file (to-str (get "FILE_PATH"))))

; (print (get "file"))

(load-file (get "file"))


