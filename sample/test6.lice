(if (file-exists? "save")
    (write-file (file "save") "1")
)

(set "var" (str->int (read-file (file "save"))))

(write-file (file "save")
            (+ 1 (get "var"))
)

(print (get "var"))

; evaluate this file

(set "file" (file (to-str (get "FILE_PATH"))))

; (print (get "file"))

(load-file (get "file"))


