; standard lib

(def ! a (if a false true))

(def null? a (=== null a))
(def !null a (not (null? a)))
(def !empty? a (not (empty? a)))

(alias in? contains?)

(def unless condition a b (if condition b a))

(def until condition a (while (! condition) a))
